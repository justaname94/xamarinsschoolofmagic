<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             x:Class="SimpleLogin.LoginView">

    <StackLayout Padding="20" Spacing="20" VerticalOptions="Center">
        <Image Source="logo_intec.png"/>
        <Entry Text="{Binding Person.Username, Mode=TwoWay}" HorizontalOptions="FillAndExpand" x:Name="usernameField" Placeholder="User"/>
        <Entry Text="{Binding Person.Password, Mode=TwoWay}" HorizontalOptions="FillAndExpand" x:Name="passwordField" Placeholder="Password" IsPassword="True" />
        <Button Text="Login"
            Command="{Binding LoginCommand}"></Button>
        <Button Text="Register" 
                BackgroundColor="Transparent" 
                TextColor="Red"
                Clicked="SendToRegisterView"></Button>
        <Label Text="{Binding Errors}" TextColor="Red" TextDecorations="Underline" HorizontalOptions="CenterAndExpand" x:Name="errorField"/>
    </StackLayout>
</ContentPage>
